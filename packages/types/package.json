{
  "name": "@project/types",
  "version": "0.0.0",
  "description": "Shared TypeScript types for projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [],
  "license": "ISC",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
